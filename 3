srand(time(0));

int n;
cout « "Введите количество элементов в массиве: "; cin » n; cout « endl;
int* mas = new int[n + 1];

int d, k = 0;
for (int i = 1; i <= n; i++)
{
mas[i] = rand() % 1001 + 1000;
cout « mas[i] « endl;
d = mas[i] % 100;
if ((d / 10) % 2 == 0) k++;
}
cout « endl « k; 
